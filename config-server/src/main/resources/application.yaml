server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_REPO_URL:https://github.com/pgr866/msc-soccer-project-config}
          default-label: main
